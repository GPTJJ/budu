# Sweet Card 1.1A — Gate A1 → A2 Checkpoint

Date: 2026-09-06 (Asia/Shanghai)

- Project: budu OS / Sweet Card 1.1A
- Repository: `https://github.com/GPTJJ/budu.git`
- Branch: `codex/sweet-card-1-1a`
- Implementation HEAD: `150683db05283655482beee15cf5103c488a1432`
- Upstream: none; all listed commits are local and unpushed
- Working tree at checkpoint creation: clean before this checkpoint
- Current Gate: A1 WeChat Identity Bridge + A2 Claim Credential
- Overall: `SWEET_CARD_1_1A_A2_READY`

## Canonical authorities

- WeChat authentication identity: `(provider, appId, openId)` in `WeChatAuthIdentity`.
- Internal customer identity and all future ownership references: stable `User.id`.
- Customer login session: server-generated opaque nonce with HMAC signature and constant-time verification; only its SHA-256 verifier is stored in `CustomerSession`.
- Claim credential: separate namespaced token and proof; only SHA-256 verifiers are stored in `SweetCardClaimToken`.
- POS redemption authority remains `SweetCardCredential`; no POS credential field or legacy printed QR behavior changed.
- Unknown WeChat identities create isolated `role=customer` users. Display name, nickname, phone, username, and employee name are never merge authority.

## Candidate schema

- Migration 68: `20260906020000_sweet_card_wechat_identity_bridge`
  - `wechat_auth_identities`: unique `(provider, app_id, open_id)`, optional indexed `union_id`, indexed `user_id` FK to `User` with `RESTRICT`.
  - `customer_sessions`: unique `token_hash`, indexed `user_id` FK to `User` with `RESTRICT`, `expires_at`, optional `revoked_at`.
- Migration 69: `20260906021000_sweet_card_claim_credential`
  - `sweet_card_claim_tokens`: unique `token_hash`, unique `proof_hash`, account and issuer FKs with `RESTRICT`, expiry/revocation/consumption timestamps, and one-active-token partial unique index per account.
- Applied authority: Test PostgreSQL `budu_sc11a_test` only, 69 applied / 0 failed.
- Production PostgreSQL remains `budu_bj006`, 67 applied / 0 failed. Migrations 68 and 69 were not applied to Production.

## Completed evidence

- Real MiniProgram `wx.login` through Test CloudBase and Test API resolved one stable internal customer identity.
- Two concurrent login codes for the same real WeChat identity converged on one identity row and one `User.id`.
- Ten further real logins resolved the same customer reference.
- Reused code and invalid code returned 401; invalid customer session returned 401.
- Direct backend identity spoof fields returned `IDENTITY_AUTHORITY_SPOOF_REJECTED`; the CloudBase contract strips unsupported identity fields.
- Test DB after A1: one customer user, one identity row, one distinct identity user, 14 hashed session records, zero customer authority violations.
- A1-10 real second-WeChat evidence is prepared as a manual matrix because USER_B was not available. Synthetic identity isolation passed without being represented as real USER_B evidence.
- A2 route tests passed authenticated resolve, repeated read-safe resolve, invalid credential denial, invalid session denial, revoked credential denial, and exact safe DTO fields.
- 128 issued Test credentials were all unique; raw tokens/proofs were held only in process memory, persisted hashes were unique, and stored values never equaled raw values.
- Test logs and source scan found zero raw claim tokens. AppSecret source scan found zero matches.
- Every A2 fixture was `ACCEPTANCE_TEST` and was removed. Final Test economic state: zero accounts, ledger rows, redemptions, refunds, bindings, and claim tokens.
- A3 claim/binding relationship was not created and no ownership or binding mutation occurred.

## Deployment and rollback evidence

- Test API image: `budu-test-api:sc11a-a1a2-150683d`; current container is healthy with restart count 0.
- Test authority endpoint: SHA `150683db0528`, database `budu_sc11a_test`, Migration 69 / 0 failed.
- Test CloudBase environment: `budu-test-d8gwb4xwy41dc6c61`; only `sweetCardApi` was deployed for the fixed A1/A2 contracts.
- Test database pre-Migration-68 backup: `/opt/budu-test/sc11a/backups/a1a2-pre-m68-20260906T021540Z/budu_sc11a_test-m67-pre-a1a2.dump`; SHA-256 `a17ec91068babe35f21f0dcd5a0bca091a79307c5b46608d1496763b7c2e3bef`.
- A first Test API switch exposed stale host `runtime.env` values and failed DB authentication. The prior healthy Test container was restored immediately. The host file was recovered from that healthy container and the new image then started successfully. The failed container and stale-file backup were retained for audit.
- A diagnostic earlier exposed Test DB/JWT values in tool output; both were rotated before this checkpoint. Production credentials and the AppSecret were not exposed.
- No Production image, container, CloudBase function, database schema, or business data was deployed or changed.

## Regression evidence

- MiniProgram and CloudBase contract regression: 44/44 PASS.
- A final real `wx.login` verified the HMAC-signed session format and the signed session verification path against the current Test API.
- POS, Cash, WeChat, Alipay regression group: 100/100 PASS.
- Sweet Card core and A0.6 harness: 62 applicable tests PASS; Store Availability full isolated matrix PASS.
- Payroll regression PASS; Transfer workflow PASS; Product import/authority contract 10/10 PASS.
- The older Product Group workflow exposed a pre-existing version assertion mismatch after group membership increments the product version. It is outside this Gate and was preserved without an unrelated code change.
- Production denial-only Store Availability regression PASS for all four direct stores; no real payment was created.
- Production public/internal health PASS, DB PASS, one Production DB writer, runtime restart count 0.
- Production reconciliation at checkpoint: Ledger 500,090 cents, account balance 500,090 cents, delta 0.

## Scope boundary and next action

Prepared A1-10 manual matrix when USER_B becomes available:

1. Open the Test MiniProgram with a genuinely different WeChat account.
2. Run one `wx.login` through the fixed Test CloudBase `wechatLoginSession` action.
3. Compare only a boolean that its returned customer reference differs from USER_A; never print code, OpenID, `User.id`, or the customer session.
4. Verify Test DB aggregate counts become two customer users, two identity rows, and two distinct identity users, with zero customer authority violations.
5. Record the result as real USER_B evidence. Do not substitute a synthetic OpenID.

- Production touched: NO for deploy/schema/business data; read-only and denial-only verification only.
- Payment status: unchanged; no real payment/refund/redemption test.
- A3, 1.1B, MiniProgram release/upload, Production rollout, Production migrations, balance, ledger, payment, order, and POS credential semantics remain prohibited.
- Next exact action: authorize and begin A3 — Claim / Binding Service. Do not auto-enter A3.

Uncommitted work and committed but unpushed work cannot be recovered from another device through Git. No unknown local changes were discarded. Production claims above were directly verified on 2026-09-06 and must be revalidated before any later Production action.
