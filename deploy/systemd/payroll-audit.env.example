# Set this to the day the server-side scheduler becomes authoritative.
# It prevents catch-up from resending periods completed by the previous scheduler.
PAYROLL_AUDIT_SCHEDULER_START_DATE=YYYY-MM-DD
