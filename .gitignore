# 依赖与构建产物
node_modules
dist
dist-ssr
test-results/
playwright-report/
*.local

# 运行数据与密钥（包含用户账号、密码哈希、JWT 密钥，切勿上传）
server/data

# 本地工具（cpolar / cloudflared 等大文件）
tools

# 临时文件
.DS_Store
npm-debug.log*
_shot
_*inspect*
.codex-tmp/
.cloudbase/
.agents/skills/

.tmp-*
_tmp*
.vercel
.env*

# 备份快照与证书（含敏感数据，切勿提交）
backups/
deploy/certs/

# 环境变量模板需要入库
!.env.example
